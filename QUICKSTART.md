# 🚀 Быстрый старт

## Что уже готово

✅ Базовая структура проекта (Backend + Frontend)  
✅ Docker Compose конфигурация  
✅ API endpoints для онбординга, дневников, клонов  
✅ Интеграция с OpenAI API  
✅ Telegram Web App SDK интеграция  
✅ Базовая валидация Telegram initData  

## Что нужно сделать

### 1. Создать Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен бота

### 2. Настроить .env файл

Файл `.env` уже создан с вашим OpenAI API ключом. Нужно добавить:

```bash
# Откройте .env и заполните:
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_SECRET_KEY=сгенерируйте_случайную_строку
```

Для генерации секретного ключа:
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 3. Запустить проект

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов backend
docker-compose logs -f backend
```

### 4. Создать таблицы БД

```bash
# Войдите в контейнер
docker-compose exec backend bash

# Создайте первую миграцию
alembic revision --autogenerate -m "Initial migration"

# Примените миграции
alembic upgrade head
```

Или создайте таблицы вручную через SQL (см. `DATABASE_AND_AI_TRAINING.md`)

### 5. Развернуть Frontend

**Вариант 1: Vercel (рекомендуется)**

```bash
cd frontend
npm install
npm run build

# Затем задеплойте на Vercel
vercel
```

**Вариант 2: Локально для тестирования**

```bash
cd frontend
npm install
npm run dev
```

### 6. Настроить Mini App в Telegram

1. Получите URL вашего frontend (например: `https://your-app.vercel.app`)
2. Откройте @BotFather
3. Используйте команду `/setmenubutton`
4. Выберите вашего бота
5. Укажите URL вашего Mini App

## Проверка работы

1. Откройте вашего бота в Telegram
2. Нажмите кнопку меню (если настроена)
3. Должно открыться Mini App
4. Пройдите анкету (имя, возраст, город, пол)
5. Создайте первый дневник

## Структура проекта

```
newProject/
├── backend/              # FastAPI backend
│   ├── app/
│   │   ├── api/         # API endpoints
│   │   ├── core/        # Конфигурация
│   │   ├── models/      # SQLAlchemy модели
│   │   └── services/    # Бизнес-логика
│   └── alembic/         # Миграции БД
├── frontend/            # Next.js Mini App
│   └── app/             # Страницы и компоненты
├── docker-compose.yml   # Docker конфигурация
└── .env                 # Переменные окружения
```

## Следующие шаги

После базовой настройки можно добавить:
- Страницу записи дневника (голос + текст)
- Страницу вопросов клону
- Систему совместимостей
- Поиск совпадений потребностей

Все детали в `DEVELOPMENT_PLAN.md`

## Проблемы?

Смотрите `SETUP.md` для подробных инструкций по настройке.
