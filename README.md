# Платформа ИИ-клонов для автоматического дейтинга и взаимопомощи

Telegram Mini App для создания ИИ-клонов пользователей через голосовые дневники.

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose
- Node.js 18+ (для локальной разработки frontend)
- Python 3.11+ (для локальной разработки backend)

### Установка

1. **Клонируйте репозиторий и перейдите в директорию:**
```bash
cd newProject
```

2. **Создайте файл `.env` на основе `.env.example`:**
```bash
cp .env.example .env
```

3. **Заполните переменные окружения в `.env`:**
   - `OPENAI_API_KEY` - ваш ключ OpenAI API
   - `TELEGRAM_BOT_TOKEN` - токен Telegram бота (получите у @BotFather)
   - `TELEGRAM_SECRET_KEY` - секретный ключ для валидации (сгенерируйте случайную строку)

4. **Запустите сервисы через Docker:**
```bash
docker-compose up -d
```

5. **Примените миграции базы данных:**
```bash
docker-compose exec backend alembic upgrade head
```

6. **Настройте Telegram бота:**
   - Откройте @BotFather в Telegram
   - Используйте команду `/setmenubutton` для вашего бота
   - Укажите URL вашего Mini App (после деплоя frontend)

### Разработка

#### Backend (FastAPI)
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload
```

#### Frontend (Next.js)
```bash
cd frontend
npm install
npm run dev
```

## 📁 Структура проекта

```
newProject/
├── backend/          # FastAPI backend
│   ├── app/
│   │   ├── api/     # API endpoints
│   │   ├── core/    # Конфигурация, безопасность
│   │   ├── models/  # SQLAlchemy модели
│   │   ├── services/ # Бизнес-логика
│   │   └── schemas/  # Pydantic схемы
│   ├── alembic/     # Миграции БД
│   └── main.py      # Точка входа
├── frontend/        # Next.js Mini App
│   ├── app/         # Страницы и роутинг
│   ├── components/  # React компоненты
│   ├── lib/         # Утилиты и API клиент
│   └── public/      # Статические файлы
└── docker-compose.yml
```

## 🔐 Безопасность

⚠️ **ВАЖНО:** Никогда не коммитьте файл `.env` в Git! API ключи должны храниться только в переменных окружения.

## 📚 Документация

- [План разработки](./DEVELOPMENT_PLAN.md)
- [База данных и обучение](./DATABASE_AND_AI_TRAINING.md)
- [Анализ текста и личности](./TEXT_ANALYSIS_AND_PERSONALITY.md)
- [Идея проекта](./PROJECT_IDEA.md)

## 🛠 Технологии

- **Backend:** Python 3.11+, FastAPI, PostgreSQL, Redis, Celery
- **Frontend:** Next.js, TypeScript, Tailwind CSS
- **ИИ:** OpenAI GPT-4, Whisper API
- **Инфраструктура:** Docker, Docker Compose

## 📝 Лицензия

Проект в разработке.
